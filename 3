// ConsoleApplication5.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "iostream"
using namespace std;


int main()
{
	setlocale(LC_ALL, "ru");
	int x,b,n;
	cin >> x;
	cin >> b;
	int *a = new int[x];
	for (int i = 0; i < x; i++){
		cin >> a[i];
	}
	for (int i = 0; i < x; i++){
		if (a[i]<b){
			n++;
		}
	}
	int *a_ = new int[n];
	for (int i = 0; i < n; i++){
		if (a[i] >= b){
			a_[i]
		}
		cout << a[i] << endl;
	}
	delete[] a;
	return 0;
}

